<% title = `${cat.name}` %>
  
<nav> 
     <div class="flex-container">
        <div class="main-nav pad">
    
<div>
    <img src="..<%= cat.image %>" height="auto" width="50%"/>

</div>

<!-- COMMENTS FORM BELOW -->
<!-- <form action="/html/tags/html_form_tag_action.cfm" method="post">
    <div>
    <textarea name="comments" id="comments" style="font-family:sans-serif;font-size:1.2em;">
    Add a comment...
    </textarea>
    </div>
    <input type="submit" value="Post Comment">
    </form> -->

<!-- RATINGS SLIDER -->
<form action="/cat/detail" method="POST">
    <label for="vol">Rating (1 - 10):</label>
    <input type="range" id="vol" name="vol" min="1" max="10" step="1" oninput="this.nextElementSibling.value = this.value; console.log(vol.value); console.log(typeof vol.value); console.log(document)"/> 
  <output></output> &nbsp;
    <input type="submit" value="Submit" name="rating">
    <input type="hidden" name="id" value="<%= cat._id %>">
    </form>

<div class="catDetails">
<div>
    Age: <%= cat.age %>
</div>

<div>
    Breed: <%= cat.breed %>
</div>

<div>
    Rating: <%= cat.rating %> 
</div>

<div>
    Likes: <%= cat.likes %>
</div>

<div>
    Dislikes: <%= cat.dislikes %>
</div>

<div>
    Joined: <%= moment(cat.createdAt).fromNow() %>
</div>

<div>
   Last updated: <%= moment(cat.updatedAt).fromNow() %>
</div>

<br>

<% cat.user.forEach(function(user) { %>

<div> Owner: <%= user.firstName + " " + user.lastName %></div>
<div> Joined: <%= moment(user.createdAt).fromNow() %></div>
<hr>
<!-- EDIT REMOVED FROM INDEX AND PLACED IN DETAIL -->
<a href="/cat/edit?id=<%= cat._id %>">Edit</a>
<% }) %>

    </div>
    </div>
</div>

</div>  
</nav>